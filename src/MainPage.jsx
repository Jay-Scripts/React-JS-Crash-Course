const Main = () => {
  return (
    <main className="w-screen scroll-smooth">
      {/* HERO SECTION */}
      <section
        id="home"
        className="min-h-screen flex items-center justify-center text-white bg-gradient-to-r from-gray-900 to-gray-800"
      >
        ...
      </section>

      {/* ABOUT SECTION */}
      <section
        id="about"
        className="min-h-screen flex flex-col justify-center items-center px-6 scroll-mt-24"
      >
        ...
      </section>

      {/* PROJECTS SECTION */}
      <section
        id="projects"
        className="min-h-screen flex flex-col justify-center px-6 bg-gray-100 scroll-mt-24"
      >
        ...
      </section>

      {/* SKILLS SECTION */}
      <section
        id="skills"
        className="min-h-screen flex flex-col justify-center px-6 scroll-mt-24"
      >
        ...
      </section>

      {/* CONTACT SECTION */}
      <section
        id="contact"
        className="min-h-screen flex flex-col justify-center items-center px-6 scroll-mt-24"
      >
        ...
      </section>
    </main>
  );
};

export default Main;
